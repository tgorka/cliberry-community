{
  "$schema": "http://json-schema.org/schema",
  "id": "cliberry-schematics#community",
  "title": "cliberry-schematics community Options Schema",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": "0"
      },
      "alias": "e",
      "description": "Contact email for code of conduct. Empty by default."
    },
    "repositoryUrl": {
      "type": "string",
      "alias": "e",
      "description": "Contact email for code of conduct. Empty by default."
    },
    "contributing": {
      "type": "boolean",
      "$default": true,
      "alias": "c",
      "description": "Do generate contributing. By default true."
    },
    "codeOfConduct": {
      "type": "boolean",
      "$default": true,
      "alias": "coc",
      "description": "Do generate code of conduct. By default true."
    },
    "pullRequest": {
      "type": "boolean",
      "$default": true,
      "alias": "pr",
      "description": "Do generate pull request template. By default true"
    },
    "bugReport": {
      "type": "boolean",
      "$default": true,
      "alias": "br",
      "description": "Do generate bug report template. By default true."
    },
    "featureRequest": {
      "type": "boolean",
      "$default": true,
      "alias": "fr",
      "description": "Do generate feature request. By default true"
    },
    "custom": {
      "type": "boolean",
      "$default": true,
      "alias": "cm",
      "description": "Do generate custm template. By default true"
    }
  },
  "required": [
    "email"
  ]
}
